"""Pytest tests for the Knowledge Hub Quality Evaluation Platform.

Layer marker: ``quality`` (registered in ``pyproject.toml``).

These tests use mock search results — no real ChromaDB/BM25 index is
required, so the suite is fast and deterministic.
"""
